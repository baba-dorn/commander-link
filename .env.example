# Public web configuration
VITE_API_BASE_URL=http://localhost:8787

# Worker-only secrets. NEVER expose these to browser or Electron renderer.
METERED_REALTIME_KEY_ID=sk_id_replace_me
METERED_REALTIME_SECRET=sk_secret_replace_me

# Optional deployment configuration
APP_ORIGIN=http://localhost:5173
ROOM_TTL_SECONDS=21600
TOKEN_TTL_SECONDS=3600
MAX_ROOM_PEERS=4
